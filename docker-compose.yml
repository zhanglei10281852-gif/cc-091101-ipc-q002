services:
  # 运行演示
  ipc-demo:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ipc-demo
    privileged: true
    volumes:
      - /tmp:/tmp
    ipc: host
    tty: true
    stdin_open: true
    command: demo
    restart: "no"

  # 运行单元测试
  ipc-test:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ipc-test
    privileged: true
    volumes:
      - /tmp:/tmp
    ipc: host
    tty: true
    command: test
    restart: "no"
    profiles:
      - test

  # 运行 Unix Domain Socket serve/request 集成测试
  ipc-integration:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ipc-integration
    privileged: true
    volumes:
      - /tmp:/tmp
    ipc: host
    command: integration
    restart: "no"
    profiles:
      - test
